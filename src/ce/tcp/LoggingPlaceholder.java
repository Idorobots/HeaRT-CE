package ce.tcp;

/**
 * Idk what about logging, an inline refactor is easy, so this should do for now I guess
 */
class LoggingPlaceholder {
    public static void info(String s) {
        System.out.println(s);
    }
}
